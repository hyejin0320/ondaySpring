<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ondayspring.list.listDAO">
    <resultMap id="list" type="com.ondayspring.list.listVO">
        <result column="book_id" property="book_id"/>
        <result column="title" property="title"/>
        <result column="category" property="category"/>
        <result column="price" property="price"/>
        <result column="insert_date" property="insert_date"/>
    </resultMap>
    <resultMap id="user" type="com.ondayspring.list.UserBasicVO">
        <result column="userId" property="USER_ID"/>
        <result column="userName" property="USER_NM"/>
        <result column="userPassword" property="USER_PWD"/>
        <result column="loginFailed" property="LOGIN_FAILED_NM"/>
    </resultMap>
    
    <select id="getUser" parameterType="com.ondayspring.list.UserBasicVO" resultMap="user">
        SELECT
                USER_ID, USER_NM, USER_PWD, LOGIN_FAILED_NUM, FIRST_MENU_URL
        FROM
                TB_USER_BAS
        WHERE
                USER_ID=#{userId}
    </select>

    <select id="getAllContents" resultMap="list">
        SELECT
                *
        FROM
                book
    </select>

</mapper>